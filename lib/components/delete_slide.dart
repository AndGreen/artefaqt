import 'package:flutter/material.dart';

class DeleteSlidable extends StatelessWidget {
  const DeleteSlidable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
